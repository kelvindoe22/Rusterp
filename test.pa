PROGRAM Part10;
VAR
   number     : INTEGER;
   a, b, c, x, d : INTEGER;
   y          : REAL;

BEGIN {Part10}
   BEGIN
      number := 2;
      a := number;
      b := 10 * a + 10 * number DIV 4;
      c := a - - b;
      d := 9
   END;
   y := 299 / 7 + 3.14;
   x := 11;

   PROCEDURE Alpha(a : INTEGER);
      VAR 
         y : INTEGER;
         x : REAL;
   BEGIN
      x := a + x + y;
   END;

   { writeln('a = ', a); }
   { writeln('b = ', b); }
   { writeln('c = ', c); }
   { writeln('number = ', number); }
   { writeln('x = ', x); }
   { writeln('y = ', y); }
END.  {Part10}